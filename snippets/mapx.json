{
  "map循环": {
    "prefix": "cm.mapx",
    "body": [
      "{${1:items}.map((${2:item}, index) => (",
      "  <View key={index}>",
      "    ${2:item}",
      "  </View>",
      "))}"
    ],
    "description": "map循环"
  }
}
